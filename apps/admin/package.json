{
  "name": "admin",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "clean": "del .turbo && del node_modules && del dist",
    "dev": "vite --host --port 6080 --clearScreen false",
    "lint": "tsc --noEmit && eslint \"src/**/*.ts*\""
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^17.0.0",
    "@types/react-dom": "^17.0.0",
    "@vitejs/plugin-react": "^2.1.0",
    "eslint": "^7.32.0",
    "eslint-config-custom": "workspace:*",
    "tsconfig": "workspace:*",
    "typescript": "^4.8.3",
    "vite": "^3.1.0"
  }
}
